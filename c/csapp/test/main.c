#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void foo() {
  
}

int main(int argc, char *argv[]) {
  char str[80] = { 1 };
  int len = 80;
  fgets(str, 80, stdin);

  return 0;
}
