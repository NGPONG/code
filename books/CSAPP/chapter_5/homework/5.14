A:
  无论实现任何版本的内积过程，其格式都属于 [X * 1] 的循环展开，也就是说，数据
  相关的关键关键路径中任然为 [n] 个加法的依赖计算，无法充分利用现代处理器的并
  行性能，即便在跑满流水线的情况下，其 CPE 的下界也不能超过(由 [Figure 5-12] 
  可知 intel i7 对于不同运算之间的算术性能标准) 1.0

B:
  引用 A 的解答，替换为浮点数的 CPE 下界
