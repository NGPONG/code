spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

server:
  port: 8882

management:
  endpoints:
    web:
      exposure:
        include: *

feign:
  sentinel:
    enabled: true